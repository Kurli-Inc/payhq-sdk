// Keep this in sync with package.json "version".
// (Reading package.json at runtime is avoided to keep the published
// dist a clean tree without a JSON import dependency.)
/** SDK version used in User-Agent and package exports. */
export const SDK_VERSION = '0.1.0';
